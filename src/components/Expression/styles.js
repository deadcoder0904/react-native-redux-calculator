import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  question: {
    color: "#fafafa",
    fontSize: 16,
    marginTop: 25,
    padding: 10
  }
});

export default styles;
